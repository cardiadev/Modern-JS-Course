const producto = "Monitor 20 Pulgadas";

console.log(producto);

// Conocer la cantidad de letras de la cadena de text
console.log(producto.length);

// indexOf funciona para saber si contiene la misma palabra o caracteres que se le pase como parametro -> sensible a mayusculas
console.log(producto.indexOf("Pulgadas"));

// includes funciona para saber si en el string incluye el parametro que se le pase -> Sensible a mayusculas
console.log(producto.includes("Tablet"));
console.log(producto.includes("Monitor"));
